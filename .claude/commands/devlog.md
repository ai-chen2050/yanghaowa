---
description: 把本次会话的改动追加进 CHANGELOG.md / DEVLOG.md，并检查是否有该沉淀的坑
---

整理本次会话的产出：

1. **CHANGELOG.md** —— 在顶部插入今天日期的条目（已有今天的就追加到它下面）。
   每条写清 *改了什么* 和 *为什么*，数值调整必须写出旧值→新值。
   格式：`- <模块>：<变更>（原因 / 影响）`
2. **DEVLOG.md** —— 一句话记录当前进度与下一步卡点，供下次会话冷启动用。
3. **坑的沉淀** —— 如果本次修复的问题满足"下次还会踩"，按 `harness-upkeep` 技能的决策表
   写进 CLAUDE.md / rules / docs 的对应位置，并在输出里告诉我写到哪了。
4. 如果本次改动让 CLAUDE.md 里某条陈述失效了（路径变了、命令变了、架构变了），直接改掉它。

不要为了凑数写空条目；这次没有值得记的就直说。
