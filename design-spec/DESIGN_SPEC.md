# 养好娃 · UI 设计规范

> 供 AI / 开发直接实施。精确令牌见同目录 `tokens.json`（机器可读）；本文写**规则、结构与逐屏规格**。
> 当前基线设计稿：`design-spec/html/v1.html`
> 最后更新：2026-09-06

---

## 0. 全局通则

- 字体：`<填：中文用 PingFang SC / Hiragino Sans GB / system-ui；游戏用具体字库文件>`
- 字号阶梯：见 `tokens.type`。大字模式全局 ×`tokens.scale.largeTextScale`。
- 间距：页面左右 `space.page`；卡片内边距 `space.cardPad`；卡片纵向间距 `space.cardGap`。
- 触控目标 ≥ `scale.minTapTarget`（大字模式 ≥52）。
- 语义色（跨主题恒定）：成功/警告/危险/信息 见 `tokens.semantic`。
- **CJK 标签禁止折行**：所有行内标签单行 + 省略号。
- 页头模式：`<填：大标题+副标题在左，圆形图标钮在右 / 居中标题 / 无页头>`

### 禁令
1. 代码里不出现字面量色值，一律取令牌。
2. 暗色不写死浅色值，取色必须经主题解析。
3. 不新增未登记的字号 / 圆角 / 间距——需要新值先加进 tokens.json。

---

## 1. 主题

### 1.1 default · light
令牌见 `tokens.themes.default.light`。特征：`<填：投影/描边/渐变的整体气质，一句话>`

### 1.2 default · dark
令牌见 `tokens.themes.default.dark`。映射规则：`<填：投影去除、强调色提亮、卡片提亮一档 等>`

> 加新主题时：复制本节，只写与 default 的**结构差异**（哪些组件换了形态），
> 数值一律进 tokens.json 的新主题节点，键集与 default 保持一致。

---

## 2. 逐屏结构

> 每屏从上到下、从外到内编号，实现时逐条打勾。隐藏交互（长按 / 空白区点击 / 手势）也要写。

### 2.1 <屏幕名>
1. 页头：标题「」+ 副标题「」+ 右侧图标钮 <n> 个
2. <模块>：…
3. <模块>：…
4. 隐藏交互：<长按 = ？ 空白点击 = ？>

### 2.2 <屏幕名>
1. …

---

## 3. 大字 / 无障碍模式

- 全局文字 ×`scale.largeTextScale`；主要触控目标提到 52+
- 隐藏次要入口：`<列出来>`
- 关键信息保留大字呈现：`<列出来>`

---

## 4. 动效规范

参数见 `tokens.motion`。

- 页面卡片入场：fadeIn + slideY，逐卡 stagger
- 数值变化：补间，不跳变
- 折叠展开：AnimatedSize / 尺寸补间
- **所有动画可被点击跳过**（重复流程尤其重要）

---

## 5. 变更记录

| 日期 | 变更 | 影响 |
|---|---|---|
| 2026-09-06 | 初版 | — |
